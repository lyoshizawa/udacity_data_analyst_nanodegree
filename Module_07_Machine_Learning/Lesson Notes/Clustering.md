
sklearn.clusters.KMeans

n_clusters:  should set this value yourself ** this is the most import variable

max_iter: number of times to iterate the algorithm.  Default: 300 times
n_init: number of times to initialize the algorithm.  Default: 10 times

Initialization can affect the clustering
Local Minimum - when the initialization results in suboptimal clustering
